import { YosrkhadijaPipe } from './yosrkhadija.pipe';

describe('YosrkhadijaPipe', () => {
  it('create an instance', () => {
    const pipe = new YosrkhadijaPipe();
    expect(pipe).toBeTruthy();
  });
});
